---
const {code, startFrom=1, lineNumbers="", stretch=false, language="js", path=null} = Astro.props;
const escaped = code.replace(/</g,"&lt;").replace(/>/g,"&gt;");
/*
   data-noescape 
*/
---
<pre class={stretch ? `r-stretch ${language}`:language}><code data-trim data-ln-start-from={startFrom} data-line-numbers={lineNumbers}>
{escaped}
</code></pre>

   {path && <p><a class="codelink" href=`https://github.com/jonasholbech/courses-s2022/tree/main/public/snippets/${path}`>github</a></p>}
